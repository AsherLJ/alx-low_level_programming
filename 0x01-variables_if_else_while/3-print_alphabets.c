#include<stdio.h>
/**
 * main - Entry
 * Return: Always 0
 */

int main(void)
{
	char q;
	char p;

	q = 'a';
	p = 'A';
		while
			(q <= 'z') {
				putchar(q);
				q++;
			}
		while
			(p <= 'Z') {
				putchar(p);
				p++;
			}
		putchar('\n');
		return (0);
}
