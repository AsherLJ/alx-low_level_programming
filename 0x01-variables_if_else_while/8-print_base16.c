#include<stdio.h>
/**
 * main -Entry
 * Return: Always
 */
int main(void)
{
	char q;
	int p;

	q = 'a';
	while
		(p < 10) {
			putchar(p + '0');
			p++;
		}
	while
		(q <= 'f') {
			putchar(q);
			q++;
		}
	putchar('\n');
	return (0);


}
